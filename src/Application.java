public class Application {
    /**
     * The running application, does all processing.
     */
    public Application() {
        PanelProcessing window = new PanelProcessing();
        window.frame.setVisible(true);
    }
}
